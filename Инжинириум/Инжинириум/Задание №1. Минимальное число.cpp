/*
#include <iostream>
using namespace std;
int main()
{
	setlocale(LC_ALL, "rus");
	float a, b, c, min = 0;
	cout << "Введите 3 числа" << endl;
	cin >> a;
	cin >> b;
	cin >> c;
	cout << "Минимальное число:";
	if (a <= b && b <= c)
	{
		min = a;
		cout << min << endl;
	}
	else if (b <= a && a <= c)
	{
		min = b;
		cout << min << endl;
	}
	else if (c <= b && b <= a)
	{
		min = c;
		cout << min << endl;
	}
}
*/